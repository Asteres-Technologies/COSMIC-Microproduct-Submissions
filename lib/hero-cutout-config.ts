// HERO TEXT CUTOUT CONFIGURATION
// Edit these values to change the glass cutout effect

export const HERO_CUTOUT_CONFIG = {
  // Text size
  fontSize: 240,
  
  // Fill color inside the cutout (white tint)
  fillColor: 'rgba(119, 119, 119, 0.3)',
  
  // Gradient stroke outline
  strokeWidth: 3,
  
  // Inner shadow settings
  shadowBlur: 12,          // Softness of shadow
  shadowOffsetX: 15,       // Horizontal position
  shadowOffsetY: 20,       // Vertical position
  shadowOpacity: 0.9,      // Darkness (0-1)
  
  // Blur effect inside cutout
  cutoutBlur: 1,
  
  // Mask fill color (controls cutout shape in mask)
  maskFill: '#1d1d1d54'
};
